package com.gson.algo.tree;

/**
 * 范型只有 <T extends BoundingType>      使用extends的写法
 * 没有    <T implements BoundingType>   使用implements的写法
 * @param <Key>
 * @param <Value>
 */
public class BTree<Key extends Comparable<Key>, Value>{
}


